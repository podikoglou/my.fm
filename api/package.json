{
  "name": "my.fm",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "typecheck": "react-router typegen && tsc",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "dependencies": {
    "hono": "^4.12.12",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
